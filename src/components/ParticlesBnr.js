import Particles from "react-particles-js"
import particleConfig from "./partcile-config"
const ParticlesBnr = () => {
  return (
    <Particles params={particleConfig}>

    </Particles>
  )
}

export default ParticlesBnr